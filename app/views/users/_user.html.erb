<div class="col-md-3">
    <div class="thumbnail">
        <div class="caption">
            <h3><%= link_to user.username , user_path(user.username) %></h3>
            <% if current_user %>
                <p><%= action_buttons(user)%></p>
            <% end %>    
        </div>
    </div>
</div>